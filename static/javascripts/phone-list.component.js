(function () {
  'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
module('main').
component('phoneList', {
  template:
  '<div>component is working now!!!!!!345</div>',
  controller: function ABCDEFG($scope) {
      // 

    }
  });


})();